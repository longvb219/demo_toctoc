import 'dart:ui';

/// Common colors
const Color white = Color.fromRGBO(255, 255, 255, 1);

const Color lightWhite = Color.fromRGBO(255, 255, 255, 0.6);

const Color black = Color.fromRGBO(0, 0, 0, 1);

const Color transparent = Color.fromRGBO(0, 0, 0, 0);

/// Button colors
const Color logButtonReady = Color.fromRGBO(241, 11, 66, 1);

const Color logButtonOption = Color.fromRGBO(240, 240, 240, 1);

const Color followButton = Color.fromRGBO(234, 67, 89, 1);

/// Icon colors
const Color bottomNavBarUnselected = Color.fromRGBO(134, 135, 139, 1);

const Color favorite = Color.fromRGBO(253, 44, 85, 1);

const Color bookmark = Color.fromRGBO(224, 227, 0, 1);

/// Border colors
const Color buttonBorder = Color.fromRGBO(227, 227, 228, 1);

const Color navBarBorder = Color.fromRGBO(38, 38, 38, 1);

/// Text colors
const Color profileInteractLabel = Color.fromRGBO(134, 135, 139, 1);
